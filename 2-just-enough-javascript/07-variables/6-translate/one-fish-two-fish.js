// #todo

// phrase <- ' '

// phrase <- 'one' + phrase + 'fish'

// phrase <- phrase + ' two'

// phrase <- phrase + ' fish'

'use strict';

debugger;

// phrase <- ' '
let phrase = ' ';

// phrase <- 'one' + phrase + 'fish'
phrase = 'one' + phrase + 'fish';

// phrase <- phrase + ' two'
phrase = phrase + ' two';

// phrase <- phrase + ' fish'
phrase = phrase + ' fish';

console.log(phrase);
